name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    name: CI
    uses: FerrLabs/.github/.github/workflows/reusable-ci-go.yml@main
    with:
      build-paths: ./cmd/...
    secrets: inherit
