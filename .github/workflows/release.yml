name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (do not push tags or releases)'
        type: boolean
        default: false

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    uses: FerrLabs/.github/.github/workflows/reusable-ferrflow-release.yml@4215a840974a9eb611b05f1496ec99d7ba0492c0 # main
    with:
      dry-run: ${{ inputs.dry_run || false }}
    secrets:
      RENOVATE_TOKEN: ${{ secrets.RENOVATE_TOKEN }}
      FERRLABS_DISPATCH_TOKEN: ${{ secrets.FERRLABS_DISPATCH_TOKEN }}
