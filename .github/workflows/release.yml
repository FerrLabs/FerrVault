name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (do not push tags or releases)'
        type: boolean
        default: false

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    uses: FerrLabs/.github/.github/workflows/reusable-ferrflow-release.yml@79140de7b61e50a96e040457bb5b97b48a0b2c78 # main
    with:
      dry-run: ${{ inputs.dry_run || false }}
    secrets:
      RENOVATE_TOKEN: ${{ secrets.RENOVATE_TOKEN }}
      FERRLABS_DISPATCH_TOKEN: ${{ secrets.FERRLABS_DISPATCH_TOKEN }}
