name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (do not push tags or releases)'
        type: boolean
        default: false

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    uses: FerrLabs/.github/.github/workflows/reusable-ferrflow-release.yml@14e06f366c629dca1a53c363471c7345d862d7d3 # main
    with:
      dry-run: ${{ inputs.dry_run || false }}
    secrets:
      RENOVATE_TOKEN: ${{ secrets.RENOVATE_TOKEN }}
      FERRLABS_DISPATCH_TOKEN: ${{ secrets.FERRLABS_DISPATCH_TOKEN }}
