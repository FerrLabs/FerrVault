name: SBOM → Dependency-Track

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sbom:
    name: SBOM → Dependency-Track
    uses: FerrLabs/.github/.github/workflows/reusable-sbom-track.yml@main
    secrets: inherit
